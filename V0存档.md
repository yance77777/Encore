# V0存档 · 余响 Encore 项目档案

> 本文档记录「余响 Encore」项目所有重要信息，包括版本记录、GitHub 链接、敏感信息、每轮对话总结。
> **每轮对话必须更新此文件。**

---

## 项目概览

- **项目名称**：余响 Encore（演唱会足迹追踪）
- **GitHub 仓库**：https://github.com/yance77777/Encore
- **GitHub Pages**：https://yance77777.github.io/Encore/ （Source: GitHub Actions，部署 `frontend/` 目录）
- **后端部署**：HF Space `andreas777/fresheye`（待接入）
- **前端技术栈**：原生 HTML/CSS/JS，静态 JSON 数据回退
- **后端技术栈**：Node.js + Express（`backend/server.js`）

### 敏感信息
- **HF Access Token**（andreas777 账号，write 权限）：⚠️ 已脱敏，明文仅保存在本地 user_profile.md，禁止入库
  - Git remote URL 模板：`https://andreas777:<TOKEN>@huggingface.co/spaces/andreas777/fresheye`
- **网络代理**：本机 127.0.0.1:7897（git push 时通过代理：`git -c http.proxy=http://127.0.0.1:7897 push`）

---

## 目录结构

```
z:\追光\
├── frontend/              ← 前端部署目录（GitHub Pages 源）
│   ├── *.html (6个)       ← index/map/tours/gallery/identity/member
│   ├── app.js             ← 前端逻辑
│   ├── style.css          ← 样式系统
│   ├── data/              ← JSON 数据（venues/artists/concerts/users/china-map.js）
│   └── assets/
│       ├── icons/         ← logo-light.jpg / logo-dark.jpg / app-icon-v2-dust.jpg
│       └── venues/        ← 31座场馆点亮图标 + unlit/ 子目录（未点亮版本）
├── backend/               ← 后端服务（部署到 HF Space）
│   ├── server.js
│   ├── package.json
│   └── package-lock.json
├── scripts/               ← 工具脚本
│   └── convert_china_map.js  ← GeoJSON → SVG path 转换（依赖 china_geo_full.json，该文件已 gitignore）
├── .github/workflows/     ← GitHub Actions 部署工作流（deploy.yml）
├── archive/               ← 归档目录（预留）
├── .gitignore             ← 忽略 node_modules/截图/缓存/geo源数据
├── README.md
├── V0存档.md              ← 本文件（项目档案）
├── index.html             ← 根目录重定向到 frontend/index.html
└── 歌手巡演专辑数据.xlsx  ← 数据源参考文件
```

### 工作区整理状态（2026-08-03）
- 已清理：扫描截图（map_check.png / china_map_screenshot.png）、临时测试脚本（test_v04.py / test_toast.py / count.py / snap.py）均已被删除
- 已确认：无残留 .png 截图、无临时 .py 脚本
- 保留：scripts/convert_china_map.js（地图生成工具）、歌手巡演专辑数据.xlsx（数据源参考）
- .gitignore 已配置：node_modules/、*.log、截图文件、china_geo_full.json（geo 源数据过大不入库）

### 部署架构
- **前端** → GitHub Pages（静态托管，通过 GitHub Actions 自动部署 `frontend/` 目录）
- **后端** → HF Space（`andreas777/fresheye`，待接入）
- **数据策略**：API 优先 + 静态 JSON 回退（GitHub Pages 无后端时只读可用）
- **写入操作**（打卡/收藏/换肤）：需后端支持；V0.4 起 localStorage 持久化

---

## 版本管理规则（a.b.c 三位数体系）
- **a（主版本）**：重大架构升级/品牌变更/核心功能重构
- **b（次版本）**：新增功能模块/重要设计改版/数据结构调整
- **c（修订版）**：Bug修复/数据校验/图标补全/文案微调

---

## 版本历史

### V0.3.1（2026-08-02）· 数据完善 + 图标全覆盖
- 31 座场馆 Q 版图标 100% 覆盖（Seedream 生成，统一风格）
- 杭州大小莲花拆分为两个独立场馆（hz-lotus 大莲花 / hz-xiaolianhua 小莲花）
- 演唱会数据 102 场网络搜索核验
- 歌手专辑数据校验修正
- 数据完整性校验：0 错误

### V0.3.0（2026-08-01）· UI 深度重构
- 暗色调 → 舞台灯光暖白设计系统
- 暖白基底 #faf8f4 + 暖金 #c8933a 强调
- 响应式三档断点（960px / 880px / 520px）
- 全部 6 个页面共用 style.css

### V0.2.0（2026-08-01）· 多页面拆分
- 单页 index.html → 6 个独立页面
- app.js 按页面元素按需渲染 + 静态 JSON 回退
- GitHub 仓库初始化推送

### V0.1（2026-08-01）· 品牌定型
- 品牌更名：追光 Lightchaser → 余响 Encore
- 主 Icon 选定：V2 光尘版（聚光灯束 + 光尘粒子 + 暖金光池）
- 目录结构建立（assets/ backend/ archive/）

---

## V0.4 开发记录（2026-08-03）· 已完成

### 开发目标（8 项）全部完成

**1. 场馆状态交互功能** ✅
- `toggleCheckin()` 升级为双向切换：点击已点亮场馆→熄灭，点击未点亮→点亮
- localStorage 持久化（`encore-checkins`），页面刷新后状态保留
- 统计数据实时更新（Hero 统计 / 省份列表 / 场馆卡片 / 中国地图全部联动刷新）
- 视觉反馈：点亮动画（`lightUp` keyframe）、未点亮场馆显示"点击点亮"提示、toast 通知

**2. 内容准确性保障** ✅
- 修正 users.json 中周杰伦错误 checkin：`hz-xiaolianhua`（小莲花，Jay 从未在此演出）→ `jn-aoti`（济南奥体，2025-09-19 嘉年华巡演）
- 全站搜索 `hz-xiaolianhua` / `小莲花`，确认仅 G.E.M. 2025-11-01 在小莲花演出（正确数据）
- 版本号统一更新：所有 HTML / CSS / JS / server.js / package.json / README

**3. 页面底部异常黑色块修复** ✅
- **根因**：toast 元素 `translateY(120%)` 仅移动 43px，不足以完全隐藏到视口外（需 68px+）
- **修复**：`translateY(300%)` + `opacity:0` + `pointer-events:none`
- Playwright 验证：隐藏态 top=940px（视口 900px），完全不可见

**4. 品牌 ICON 集成** ✅
- Seedream 生成亮色版 `logo-light.jpg`（暖金光尘粒子 + 奶白背景）
- Seedream 生成暗色版 `logo-dark.jpg`（暖金光尘粒子 + 深蓝背景）
- 集成到全部 6 个 HTML 页面的 logo 左侧
- CSS 自动切换：`html:not(.dark) .dark-only{display:none}` / `html.dark .light-only{display:none}`

**5. 歌手专辑信息完善** ✅
- 13 位歌手共 139 张专辑数据完整（artists.json 已有名称+年份）
- 收藏展览馆新增"全部专辑"标签页，展示全部 139 张专辑
- 专辑卡片设计：歌手色背景 + 首字标识 + 年份徽章 + 收藏状态
- 点击专辑可收藏/取消收藏，localStorage 持久化（`encore-collections`）

**6. 交互功能优化** ✅
- 场馆卡片：双向点击切换（点亮/熄灭）+ 动画反馈
- 专辑卡片：点击收藏/取消 + 实时状态更新
- 主题切换：点击按钮即时切换明暗模式 + 持久化
- 皮肤切换：已有功能保留（13 种歌手主题色）

**7. 代码优化与多端适配** ✅
- 响应式断点：960px / 880px / 520px，新增主题按钮和 logo 的移动端适配
- 暗色模式全元素适配：header / cards / map / toast / 地图省份 / 图例
- localStorage 三重持久化：`encore-theme`（主题）/ `encore-checkins`（打卡）/ `encore-collections`（收藏）
- API 优先 + 本地回退：无后端时所有交互功能仍可正常使用

**8. 明暗两套 UI 体系** ✅
- CSS 变量体系：`html.dark` 覆盖全部基底/品牌/边框/阴影变量
- 明暗双版本 ICON：自动根据主题切换显示
- 主题切换按钮：🌙（亮色模式显示月亮）/ ☀（暗色模式显示太阳）
- 全页面元素适配：header 背景 / 卡片底色 / 地图省份 / 文字颜色 / 阴影层级
- 切换持久化：localStorage 记录用户选择，刷新/跨页面保持一致

### V0.4 数据统计
| 类别 | V0.3.1 | V0.4 | 变化 |
|------|--------|------|------|
| 场馆数量 | 31 | 31 | 不变 |
| 歌手数量 | 8 | 13 | +5（王力宏/陶喆/许嵩/李荣浩/谢霆锋） |
| 演唱会场次 | 102 | 139 | +37 |
| 专辑总数 | — | 139 | 全部展示 |
| UI 模式 | 仅亮色 | 明暗双模 | +暗色主题 |
| 场馆交互 | 仅点亮 | 点亮/熄灭 | 双向切换 |
| 数据持久化 | 无 | localStorage | 三重持久化 |
| 品牌 ICON | 无 | 明暗双版 | 集成到 logo |

### V0.4 修改文件清单
- `frontend/style.css` — 暗色主题变量 + 主题切换/ICON/动画样式 + 暗色适配
- `frontend/app.js` — 主题切换 + 场馆双向 toggle + localStorage + 专辑展览增强
- `frontend/index.html` — logo icon + 主题按钮 + 版本号 v0.4
- `frontend/map.html` — logo icon + 主题按钮 + 版本号 v0.4
- `frontend/tours.html` — logo icon + 主题按钮 + 版本号 v0.4
- `frontend/gallery.html` — logo icon + 主题按钮 + "全部专辑"标签 + 版本号 v0.4
- `frontend/identity.html` — logo icon + 主题按钮 + 版本号 v0.4
- `frontend/member.html` — logo icon + 主题按钮 + 版本号 v0.4
- `frontend/data/users.json` — 修正周杰伦错误 checkin 记录
- `frontend/assets/icons/logo-light.jpg` — 新增亮色品牌 ICON
- `frontend/assets/icons/logo-dark.jpg` — 新增暗色品牌 ICON
- `backend/server.js` — 版本号 v0.4
- `backend/package.json` — 版本号 0.4.0
- `README.md` — 版本号 + 数据统计 + 项目结构更新
- `V0存档.md` — 新建项目档案文件

---

## 对话记录

### 2026-08-03 · V0.4 发布上线（本轮）
- **用户指令**：GitHub Pages 源已切换为 GitHub Actions，直接推送线上部署
- **执行过程**：
  1. `git add` 暂存 16 个变更文件（13 修改 + 3 新增 + 1 删除旧存档）
  2. 首次提交：commit `d761868`（16 files changed, 532 insertions, 278 deletions）
  3. 首次推送被 GitHub Push Protection 拦截：V0存档.md 第18行包含 HF Token 明文
  4. 脱敏处理：将 HF Token 替换为占位符 `<TOKEN>`，明文仅保存在本地 user_profile.md
  5. `git commit --amend` 修正提交：commit `29e37df`
  6. 通过代理 `http://127.0.0.1:7897` 推送成功：`46aa9ac..29e37df main -> main`
- **部署状态**：代码已推送至 main 分支，GitHub Actions 自动触发部署到 GitHub Pages
- **访问地址**：https://yance77777.github.io/Encore/
- **安全教训**：敏感信息（token/密钥）禁止入库，已在 .gitignore 规则外通过脱敏处理规避

### 2026-08-03 · V0.4 工作区整理与归档
- **用户指令**：整理工作区文件夹，将重要信息存档到 V0存档.md（每轮对话持续更新），删除扫描产生的截图或无用缓存
- **执行结果**：
  - 扫描工作区：无 .png 截图残留、无临时 .py 脚本残留（前期已清理）
  - 确认保留文件：scripts/convert_china_map.js（地图工具）、歌手巡演专辑数据.xlsx（数据源）
  - 更新 V0存档.md 目录结构：补充 scripts/、index.html、package-lock.json 等遗漏项
  - 新增"工作区整理状态"小节，记录清理结果
- **V0.4 最终状态**：8 项开发目标全部完成，工作区整洁，存档完整

### 2026-08-03 · V0.4 开发会话
- **用户指令**：启动 V0.4 版本开发，8 项详细要求（场馆交互/内容准确性/底部修复/品牌ICON/专辑完善/交互优化/多端适配/明暗UI）
- **补充指令**：整理工作区文件夹，创建 V0存档.md，删除扫描缓存
- **已完成**：修正错误数据、定位并修复底部黑色块、CSS 暗色主题体系、生成明暗双版本ICON
- **GitHub Pages**：用户已切换部署源为 GitHub Actions，部署正常

### 2026-08-03 · GitHub Pages 部署修复 + 中国地图
- 修复 GitHub Pages 部署 README 而非 frontend 的问题（切换部署源为 GitHub Actions）
- 修复中国地图 GeoJSON 数据处理（RDP 简化错误、MultiPolygon 处理、异常图形过滤）

### 2026-08-01 ~ 02 · V0.3 ~ V0.3.1
- UI 重构、数据完善、图标全覆盖（详见版本历史）
