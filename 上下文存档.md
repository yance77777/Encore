# 上下文存档

> 本文档记录「余响 Encore」项目每次对话的简短总结，便于跨会话延续上下文。

---

## 2026-08-01 · 品牌定型与多页面拆分

### 完成事项
- **品牌更名**：追光 Lightchaser → **余响 Encore**（避免与追光动画撞名）
- **主 Icon 选定**：B 方案"聚光灯返场" → V2 光尘版（聚光灯束 + 光尘粒子 + 暖金光池）
  - 文件：`design/icon_final_v2_dust.jpg`
  - 配色：深午夜蓝 `#0f0f23` + 暖金 `#ffb547`
- **多页面拆分**：单页 index.html → 6 个独立页面
  - `index.html` 首页（Hero + 模块导航）
  - `map.html` 场馆地图
  - `tours.html` 巡演档案
  - `gallery.html` 收藏展览
  - `identity.html` 粉丝身份
  - `member.html` 会员体系
- **app.js 重构 v0.2.0**：按页面元素按需渲染 + 静态 JSON 回退（GitHub Pages 只读可用）
- **GitHub 仓库**：`https://github.com/yance77777/Encore`，初始化并推送

### 部署架构
- 前端 → GitHub Pages（静态托管）
- 后端 → HF Space（`andreas777/fresheye`，待接入）
- app.js 中 `API_BASE` 留空=本地开发；部署时填 HF Space URL

### 关键决策
- 数据加载采用"API 优先 + 静态 JSON 回退"策略，GitHub Pages 无后端时仍可只读展示
- 写入操作（打卡/收藏/换肤）需后端支持，静态模式提示"在线写入需后端支持"
- 前端文件置于仓库根目录，GitHub Pages 直接从根目录提供服务

### 待办
- [ ] 后端部署到 HF Space，开启 CORS，前端填入 `API_BASE`
- [ ] GitHub Pages 配置（Settings → Pages → main 分支根目录）
- [ ] Icon SVG 矢量版本制作并接入
- [ ] 5 个功能模块 Icon 设计
